[tool.poetry]
name = "NaomiLapaglia"
version = "0.1.1"
description = ""
authors = ["drazum <domagoj.razum1@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
customize = "naomilapaglia.overrides:admin_override"
