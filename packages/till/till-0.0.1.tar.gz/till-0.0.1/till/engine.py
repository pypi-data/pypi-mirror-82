# import libs
import subprocess
import os
import sys
from flask import Flask

# create main class
class engine():
    """
    main class that turns scripts into containers

    engine() class allows the users to create and containerize Flask APIs while
    working in their favorite Python IDE. The engine() class builds on top of
    the Flask framework.

    Parameters
    ----------


    Examples
    --------
    >>> from till import engine
    >>> 

    """