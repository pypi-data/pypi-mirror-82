#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['yaqd_control']

package_data = \
{'': ['*'], 'yaqd_control': ['bin/*']}

install_requires = \
['appdirs', 'toml', 'click', 'prettytable!=1.0.0', 'colorama', 'yaqc']

extras_require = \
{'dev': ['black', 'pre-commit'], 'tests': ['pytest', 'pytest-subprocess']}

entry_points = \
{'console_scripts': ['yaqd = yaqd_control.__main__:main']}

setup(name='yaqd-control',
      version='2020.10.0',
      description='Command line tools for inspecting and controlling yaq daemons.',
      author='yaq developers',
      author_email='git@ksunden.space',
      url='https://yaq.fyi',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
