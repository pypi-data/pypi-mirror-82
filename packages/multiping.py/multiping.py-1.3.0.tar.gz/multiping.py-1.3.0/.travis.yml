language: python
python:
  - 3.6
  - 3.7
  - 3.8
  - pypy3

jobs:
  include:
    - name: flake8
      install: pip install tox
      script: tox -e flake8
      after_script:

cache: pip
install:
  - pip install pytest coverage coveralls
script:
  - coverage run -m pytest
after_script:
  - coveralls

notifications:
  email: false
