from setuptools import setup

setup(name='basic_probability_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['basic_probability_distributions'],
      zip_safe=False)
